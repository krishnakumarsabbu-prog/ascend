import { motion } from 'framer-motion'
import { ArrowUpRight, ChartBar as BarChart3, CircleCheck as CheckCircle2, ChevronRight, CircleAlert, Clock3, FileCheck2, GitBranch, Layers as Layers3, ShieldCheck, Users, Zap } from 'lucide-react'
import type { Associate, RoleId } from '../types'
import { Badge, Button, Card, ProgressBar } from '../components/ui'

const configs: Record<Exclude<RoleId, 'EARLY_TALENT'>, { eyebrow: string; title: string; subtitle: string; metric: string; metricLabel: string; action: string; icon: typeof Users }> = {
  MENTOR_COACH: { eyebrow: 'Mentor workspace', title: 'My mentees', subtitle: 'Support your associates with focused coaching and timely interventions.', metric: '4', metricLabel: 'Active mentees', action: 'Review check-ins', icon: Users },
  ENGINEERING_EXCELLENCE_COMMITTEE: { eyebrow: 'Governance workspace', title: 'Cohort overview', subtitle: 'Monitor pathway health, assessment quality, and standards across cohorts.', metric: '86%', metricLabel: 'Cohort readiness', action: 'Open governance view', icon: ShieldCheck },
  SENIOR_LEADER_SPONSOR: { eyebrow: 'Sponsor workspace', title: 'Demand & pipeline', subtitle: 'Connect strategic engineering demand to a ready, developing talent pipeline.', metric: '18', metricLabel: 'Active placements', action: 'Review pipeline', icon: BarChart3 },
}

const accent: Record<string, string> = { MENTOR_COACH: 'blue', ENGINEERING_EXCELLENCE_COMMITTEE: 'emerald', SENIOR_LEADER_SPONSOR: 'amber' }

export function RoleWorkspace({ role, associates }: { role: Exclude<RoleId, 'EARLY_TALENT'>; associates: Associate[] }) {
  const config = configs[role]
  const color = accent[role]
  const Icon = config.icon
  return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 text-xs font-semibold uppercase tracking-wider text-blue-600">{config.eyebrow}</p><h1 className="text-2xl font-bold tracking-tight text-slate-950 md:text-3xl">{config.title}</h1><p className="mt-1 text-sm text-slate-500">{config.subtitle}</p></div><Button><Zap size={14} />{config.action}</Button></div><div className="grid gap-4 md:grid-cols-4"><Metric icon={<Icon size={17} />} value={config.metric} label={config.metricLabel} tone={color} /><Metric icon={<CheckCircle2 size={17} />} value="92%" label="On track" tone="emerald" /><Metric icon={<Clock3 size={17} />} value="7" label="Items needing review" tone="amber" /><Metric icon={<GitBranch size={17} />} value="12" label="Milestones this quarter" tone="navy" /></div><div className="grid gap-6 xl:grid-cols-[1.35fr_1fr]"><Card><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 className="text-sm font-bold text-slate-900">Talent health overview</h2><p className="mt-0.5 text-xs text-slate-400">Latest signals from your portfolio</p></div><button className="text-slate-400 hover:text-slate-700"><BarChart3 size={17} /></button></div><div className="grid gap-3 p-5 sm:grid-cols-2">{associates.map((associate, index) => <motion.div key={associate.id} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.06 }} className="group rounded-md border border-slate-200 p-4 transition-all hover:border-blue-200 hover:shadow-sm"><div className="flex items-start gap-3"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-[#0c1b33] text-[10px] font-bold text-white">{associate.name.split(' ').map((n) => n[0]).join('')}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div><p className="truncate text-xs font-bold text-slate-800">{associate.name}</p><p className="mt-0.5 truncate text-[10px] text-slate-400">{associate.team_name}</p></div><Badge className={associate.standing === 'FAST_TRACK' ? 'bg-blue-50 text-blue-700' : 'bg-emerald-50 text-emerald-700'}>{associate.standing.replace('_', ' ')}</Badge></div><div className="mt-4"><div className="mb-1.5 flex justify-between text-[10px] text-slate-500"><span>Journey progress</span><span className="font-bold text-slate-700">{Math.min(100, associate.current_month * 4)}%</span></div><ProgressBar value={Math.min(100, associate.current_month / 24)} color={associate.standing === 'FAST_TRACK' ? 'bg-blue-500' : 'bg-emerald-500'} /></div></div></div><div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-3 text-[10px] text-slate-400"><span>{associate.pathway_code}</span><span className="flex items-center gap-1 font-semibold text-blue-600 opacity-0 transition-opacity group-hover:opacity-100">Open profile <ChevronRight size={12} /></span></div></motion.div>)}</div></Card><Card><div className="border-b border-slate-100 px-5 py-4"><h2 className="text-sm font-bold text-slate-900">Review queue</h2><p className="mt-0.5 text-xs text-slate-400">Items requiring your attention</p></div><div className="divide-y divide-slate-100"><QueueItem icon={<FileCheck2 size={15} />} title="Assessment calibration" detail="3 assessments awaiting review" tone="blue" /><QueueItem icon={<CircleAlert size={15} />} title="ASM-202 waiver request" detail="Rohan Mehta · due Friday" tone="amber" /><QueueItem icon={<ShieldCheck size={15} />} title="Quarterly pathway review" detail="4 profiles ready for review" tone="emerald" /><QueueItem icon={<Layers3 size={15} />} title="Cohort trend report" detail="Updated 2 hours ago" tone="navy" /></div><div className="p-4"><Button variant="outline" className="w-full">Open review queue <ArrowUpRight size={14} /></Button></div></Card></div><Card><div className="border-b border-slate-100 px-5 py-4"><h2 className="text-sm font-bold text-slate-900">Portfolio activity</h2><p className="mt-0.5 text-xs text-slate-400">Recent changes across your workspace</p></div><div className="grid gap-4 p-5 md:grid-cols-3"><Activity title="Milestone completed" detail="Ananya Rao completed ASM-101" time="2 hours ago" tone="emerald" /><Activity title="Review requested" detail="PAY-201 needs calibration" time="Yesterday" tone="blue" /><Activity title="Pathway signal" detail="Cohort readiness increased 4%" time="2 days ago" tone="amber" /></div></Card></div>
}
function Metric({ icon, value, label, tone }: { icon: React.ReactNode; value: string; label: string; tone: string }) { const tones: Record<string, string> = { blue: 'bg-blue-50 text-blue-600', emerald: 'bg-emerald-50 text-emerald-600', amber: 'bg-amber-50 text-amber-600', navy: 'bg-slate-100 text-slate-700' }; return <Card className="p-4"><div className="flex items-center gap-3"><div className={`flex h-8 w-8 items-center justify-center rounded-md ${tones[tone]}`}>{icon}</div><div><p className="text-xl font-bold text-slate-950">{value}</p><p className="text-[11px] text-slate-500">{label}</p></div></div></Card> }
function QueueItem({ icon, title, detail, tone }: { icon: React.ReactNode; title: string; detail: string; tone: string }) { const tones: Record<string, string> = { blue: 'bg-blue-50 text-blue-600', emerald: 'bg-emerald-50 text-emerald-600', amber: 'bg-amber-50 text-amber-600', navy: 'bg-slate-100 text-slate-700' }; return <button className="flex w-full items-center gap-3 px-5 py-4 text-left transition-colors hover:bg-slate-50"><div className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-md ${tones[tone]}`}>{icon}</div><div className="min-w-0 flex-1"><p className="text-xs font-bold text-slate-800">{title}</p><p className="mt-0.5 truncate text-[11px] text-slate-500">{detail}</p></div><ChevronRight size={14} className="text-slate-300" /></button> }
function Activity({ title, detail, time, tone }: { title: string; detail: string; time: string; tone: string }) { return <div className="flex gap-3"><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${tone === 'emerald' ? 'bg-emerald-500' : tone === 'blue' ? 'bg-blue-500' : 'bg-amber-500'}`} /><div><p className="text-xs font-bold text-slate-800">{title}</p><p className="mt-1 text-[11px] leading-5 text-slate-500">{detail}</p><p className="mt-2 text-[10px] text-slate-400">{time}</p></div></div> }
